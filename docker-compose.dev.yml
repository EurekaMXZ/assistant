services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"

  redis:
    ports:
      - "127.0.0.1:6379:6379"

  kafka:
    ports:
      - "127.0.0.1:9092:9092"

  minio:
    environment:
      MINIO_API_CORS_ALLOW_ORIGIN: http://localhost:3000
